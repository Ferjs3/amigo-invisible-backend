spring.application.name=amigo-invisible-backend
server.port=8080

# --- Base de datos ---
# Crea antes la base con: CREATE DATABASE amigo_invisible CHARACTER SET utf8mb4;
spring.datasource.url=jdbc:mysql://localhost:3306/amigo_invisible?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=messi2020

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# --- CORS: origen de tu app Angular en desarrollo ---
app.cors.allowed-origin=http://localhost:4200

# --- Token de sesion simple ---
app.auth.token-expiration-days=30
